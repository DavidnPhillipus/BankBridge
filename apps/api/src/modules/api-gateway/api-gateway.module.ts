import { Module } from '@nestjs/common';

/**
 * API Gateway: orchestrates adapter calls, enforces consent, and normalizes
 * heterogeneous bank responses into canonical data models (Anti-Corruption Layer).
 */
@Module({})
export class ApiGatewayModule {}
